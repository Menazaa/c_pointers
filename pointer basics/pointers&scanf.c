#include <stdio.h>


int main(int argc, char const *argv[])
{
    int num;
    scanf("%d",&num); // the second parameter of scanf is a pointer where the value will be stored
    printf("the value of num: %d\n",num);
    return 0;
}
